{
    "title": "Heureka Crawler Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "Start URLs",
            "type": "array",
            "description": "List of category URLs to start crawling from.",
            "editor": "requestListSources",
            "default": [
                { "url": "https://www.heureka.cz/" }
            ]
        },
        "maxPages": {
            "title": "Max Pages",
            "type": "integer",
            "description": "Maximum number of pages to crawl (including both category and product pages).",
            "default": 100,
            "minimum": 1
        },
        "maxProducts": {
            "title": "Max Products",
            "type": "integer",
            "description": "Maximum number of products to scrape. Crawler will stop after reaching this limit.",
            "default": 1000,
            "minimum": 1
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration",
            "type": "object",
            "description": "Proxy settings. Residential proxies from Czech Republic are required to bypass Cloudflare protection on Heureka.cz.",
            "editor": "proxy",
            "default": { 
                "useApifyProxy": true, 
                "apifyProxyGroups": ["RESIDENTIAL"], 
                "apifyProxyCountry": "CZ"
            },
            "prefill": { 
                "useApifyProxy": true, 
                "apifyProxyGroups": ["RESIDENTIAL"], 
                "apifyProxyCountry": "CZ"
            }
        }
    },
    "required": ["startUrls"]
}
