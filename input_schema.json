{
    "title": "Heureka Crawler Input",
    "type": "object",
    "properties": {
        "startUrls": {
            "title": "Start URLs",
            "type": "array",
            "description": "List of category URLs to start crawling from.",
            "editor": "requestListSources",
            "prefill": [
                { "url": "https://www.heureka.cz/" }
            ]
        },
        "maxPages": {
            "title": "Max Pages",
            "type": "integer",
            "description": "Maximum number of pages to crawl.",
            "default": 100,
            "editor": "number"
        },
        "maxProducts": {
            "title": "Max Products",
            "type": "integer",
            "description": "Maximum number of products to scrape.",
            "default": 1000,
            "editor": "number"
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration",
            "type": "object",
            "description": "Select proxies to be used by your crawler.",
            "editor": "proxy",
            "prefill": { "useApifyProxy": true, "apifyProxyGroups": ["RESIDENTIAL"], "apifyProxyCountry": "CZ" }
        }
    },
    "required": ["startUrls", "proxyConfiguration"]
}
